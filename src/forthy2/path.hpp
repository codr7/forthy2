#ifndef FORTHY2_PATH_HPP
#define FORTHY2_PATH_HPP

#include <experimental/filesystem>

namespace forthy2 {
  using namespace std;
  namespace fs = experimental::filesystem;
  using Path = fs::path;
}

#endif
